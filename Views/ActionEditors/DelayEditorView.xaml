<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="HotKeyManager.Views.ActionEditors.DelayEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Spacing="8">
        <NumberBox Header="Dauer (ms)"
                   Value="{x:Bind ViewModel.DurationMs, Mode=TwoWay}"
                   Minimum="0"
                   Maximum="600000"
                   SmallChange="100"
                   SpinButtonPlacementMode="Inline"
                   HorizontalAlignment="Left"
                   MinWidth="220" />
        <TextBlock Text="Wartet die angegebene Zeit, bevor der nächste Baustein ausgeführt wird."
                   FontSize="12"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </StackPanel>
</UserControl>
