<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="HotKeyManager.Views.HotkeyListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotKeyManager.Views"
    Background="Transparent">
    
    <Grid Padding="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <StackPanel Grid.Column="0">
                <TextBlock Text="Meine Hotkeys" 
                           FontSize="32" 
                           FontWeight="Bold" 
                           Foreground="{StaticResource TextPrimaryBrush}" />
                <TextBlock x:Name="HotkeyCountText"
                           Text="0 Hotkeys konfiguriert" 
                           FontSize="14" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,4,0,0" />
            </StackPanel>
            
            <Button Grid.Column="1" 
                    Click="AddHotkey_Click"
                    Style="{StaticResource AccentButtonStyle}">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE710;" FontSize="16" />
                    <TextBlock Text="Neuer Hotkey" FontSize="14" />
                </StackPanel>
            </Button>
        </Grid>
        
        <!-- Hotkey List -->
        <Grid Grid.Row="1">
            <!-- Empty State -->
            <StackPanel x:Name="EmptyState" 
                        Visibility="Collapsed"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Spacing="16">
                <FontIcon Glyph="&#xE765;" 
                          FontSize="64" 
                          Foreground="{StaticResource TextSecondaryBrush}" />
                <TextBlock Text="Keine Hotkeys vorhanden" 
                           FontSize="20" 
                           FontWeight="SemiBold"
                           Foreground="{StaticResource TextPrimaryBrush}"
                           HorizontalAlignment="Center" />
                <TextBlock Text="Erstelle deinen ersten Hotkey um loszulegen" 
                           FontSize="14" 
                           Foreground="{StaticResource TextSecondaryBrush}"
                           HorizontalAlignment="Center" />
                <Button Click="AddHotkey_Click"
                        HorizontalAlignment="Center"
                        Margin="0,8,0,0"
                        Style="{StaticResource AccentButtonStyle}">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE710;" FontSize="14" />
                        <TextBlock Text="Hotkey erstellen" />
                    </StackPanel>
                </Button>
            </StackPanel>
            
            <!-- Hotkey ListView -->
            <ListView x:Name="HotkeyListView"
                      SelectionMode="None">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="Margin" Value="0,0,0,12" />
                        <Setter Property="Background" Value="Transparent" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{StaticResource SurfaceBrush}" 
                                CornerRadius="12" 
                                Padding="20">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <!-- Key Badge (klickbar zum Ausführen) -->
                                <Button Grid.Column="0"
                                        Click="ExecuteHotkey_Click"
                                        Tag="{Binding Id}"
                                        Background="#2D3748"
                                        CornerRadius="8"
                                        Padding="16,10"
                                        VerticalAlignment="Center"
                                        BorderThickness="0"
                                        ToolTipService.ToolTip="Klicken zum Ausführen">
                                    <TextBlock Text="{Binding KeyDisplayText}" 
                                               FontFamily="Consolas" 
                                               FontSize="14"
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource AccentBrush}" />
                                </Button>
                                
                                <!-- Info -->
                                <StackPanel Grid.Column="1" 
                                            VerticalAlignment="Center" 
                                            Margin="20,0">
                                    <TextBlock Text="{Binding Name}" 
                                               FontSize="16" 
                                               FontWeight="SemiBold"
                                               Foreground="{StaticResource TextPrimaryBrush}" />
                                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,4,0,0">
                                        <Border Background="#374151" 
                                                CornerRadius="4" 
                                                Padding="8,3">
                                            <TextBlock Text="{Binding Action.DisplayName}" 
                                                       FontSize="12" 
                                                       Foreground="{StaticResource TextSecondaryBrush}" />
                                        </Border>
                                        <TextBlock Text="{Binding Action.Description}" 
                                                   FontSize="13" 
                                                   Foreground="{StaticResource TextSecondaryBrush}"
                                                   VerticalAlignment="Center" />
                                    </StackPanel>
                                </StackPanel>
                                
                                <!-- Actions -->
                                <StackPanel Grid.Column="2" 
                                            Orientation="Horizontal" 
                                            Spacing="8"
                                            VerticalAlignment="Center">
                                    <ToggleSwitch IsOn="{Binding IsEnabled, Mode=OneWay}" 
                                                  OnContent="" 
                                                  OffContent=""
                                                  Toggled="ToggleSwitch_Toggled"
                                                  Tag="{Binding Id}" />
                                    <Button Background="Transparent" 
                                            Padding="10"
                                            Click="EditHotkey_Click"
                                            Tag="{Binding Id}">
                                        <FontIcon Glyph="&#xE70F;" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" />
                                    </Button>
                                    <Button Background="Transparent" 
                                            Padding="10"
                                            Click="DeleteHotkey_Click"
                                            Tag="{Binding Id}">
                                        <FontIcon Glyph="&#xE74D;" FontSize="14" Foreground="#EF4444" />
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
