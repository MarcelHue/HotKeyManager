<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="HotKeyManager.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:HotKeyManager"
    xmlns:views="using:HotKeyManager.Views"
    Title="HotKey Manager">
    
    <Grid Background="{StaticResource BackgroundBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="280" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        
        <!-- Navigation Sidebar -->
        <Border Grid.Column="0" 
                Background="#0D1B2A"
                Padding="0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                
                <!-- App Header -->
                <StackPanel Grid.Row="0" Padding="24,32,24,24">
                    <TextBlock Text="⌨️" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,12" />
                    <TextBlock Text="HotKey Manager" 
                               FontSize="22" 
                               FontWeight="Bold" 
                               Foreground="{StaticResource TextPrimaryBrush}"
                               HorizontalAlignment="Center" />
                    <TextBlock Text="Globale Tastenkürzel verwalten" 
                               FontSize="12" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0" />
                </StackPanel>
                
                <!-- Navigation Items -->
                <StackPanel Grid.Row="1" Spacing="4" Padding="12,0">
                    <Button x:Name="NavHotkeysButton" 
                            Click="NavHotkeys_Click"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Background="Transparent"
                            Padding="16,14"
                            CornerRadius="10">
                        <StackPanel Orientation="Horizontal" Spacing="14">
                            <FontIcon Glyph="&#xE765;" FontSize="18" Foreground="{StaticResource AccentBrush}" />
                            <TextBlock Text="Hotkeys" FontSize="15" Foreground="{StaticResource TextPrimaryBrush}" />
                        </StackPanel>
                    </Button>
                    
                    <Button x:Name="NavSettingsButton" 
                            Click="NavSettings_Click"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Left"
                            Background="Transparent"
                            Padding="16,14"
                            CornerRadius="10">
                        <StackPanel Orientation="Horizontal" Spacing="14">
                            <FontIcon Glyph="&#xE713;" FontSize="18" Foreground="{StaticResource TextSecondaryBrush}" />
                            <TextBlock Text="Einstellungen" FontSize="15" Foreground="{StaticResource TextSecondaryBrush}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
                
                <!-- Footer Info -->
                <StackPanel Grid.Row="2" Padding="24" Spacing="8">
                    <Border Background="#1B2838" CornerRadius="8" Padding="12">
                        <StackPanel Spacing="8">
                            <!-- Hook Status -->
                            <StackPanel Spacing="4">
                                <TextBlock Text="Hook Status" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Ellipse Width="8" Height="8" Fill="#4ADE80" VerticalAlignment="Center" />
                                    <TextBlock x:Name="StatusText" Text="Aktiv" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                                </StackPanel>
                            </StackPanel>
                            
                            <!-- Driver Status -->
                            <StackPanel Spacing="4">
                                <TextBlock Text="Kernel-Treiber" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" />
                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <Ellipse x:Name="DriverStatusIndicator" Width="8" Height="8" Fill="#6B7280" VerticalAlignment="Center" />
                                    <TextBlock x:Name="DriverStatusText" Text="Nicht installiert" FontSize="13" Foreground="{StaticResource TextPrimaryBrush}" />
                                </StackPanel>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                    <TextBlock Text="Version 1.0.0" 
                               FontSize="11" 
                               Foreground="{StaticResource TextSecondaryBrush}"
                               HorizontalAlignment="Center" />
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Main Content Area -->
        <Grid Grid.Column="1">
            <Frame x:Name="ContentFrame" />
        </Grid>
    </Grid>
</Window>
